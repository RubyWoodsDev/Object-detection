<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Object Detector</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="style.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Kaisei+Decol&display=swap" rel="stylesheet">
  </head>
  <body>


   <% if(objects.length > 0) { %>
    <img src="<%= img%>" alt="uploaded image" /><br>
   <h2> Objects found: </h2>
    <ul>
      <% for(let i = 0; i < objects.length; i++) { %>
         <li><%= objects[i].object %> <br>  -- confidence level: <%=Math.ceil(objects[i].confidence.toFixed(2) * 100)  %>%</li>
     <% } %>
    
    <% }else { %>
      <img src="<%= img%>" alt="uploaded image" />
    <h1> No objects found </h1>
    <% } %>



      <form action="/" enctype="multipart/form-data" method="post">
      <input type="file" id="inputOne" name="file-to-upload" />
      <input type="submit" id="inputTwo"  value="Upload" />
    </form>
  </body>
</html>